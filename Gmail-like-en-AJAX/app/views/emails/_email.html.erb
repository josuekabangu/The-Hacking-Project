<h3 id="email-object"><%= @email.object %></h3>
<p id="email-body" class="text-justify"><%= @email.body %></p>

<%= link_to 'Marquer comme non-lu', email_path(@email.id), remote: true, method: 'put' %>

<%= link_to 'Supprimer', email_path(@email.id), remote: true, method: 'delete' %>
